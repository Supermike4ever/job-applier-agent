# requirements.txt
boto3>=1.34.0         # AWS SDK，用于调用 Amazon Nova 模型
playwright>=1.42.0    # 核心的浏览器自动化工具
pydantic>=2.6.0       # 用于定义和校验 JSON 简历与表单数据结构
python-dotenv>=1.0.0  # 用于从 .env 文件安全读取 AWS 密钥和环境变量